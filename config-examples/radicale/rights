; Example rights file (from_file backend)
; See https://radicale.org/master.html#rights for syntax
; Allow users to read/write their own collections under /<login>/

[user-collections]
user: .+
collection: ^/(.+)(/.*)?
permission: rw

# Read-only public calendars (optional example)
[public]
user: .+
collection: ^/public(/.*)?
permission: r
